"""Custom bigraph-schema types for NFSim wrapper."""


def register_nfsim_types(core):
    """Register custom types used by NFSim processes.

    Currently uses built-in types only. This function is a hook for
    future custom type registrations (e.g. molecule count types,
    reaction rate types).
    """
    pass
